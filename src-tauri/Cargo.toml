[package]
publish = false
edition = "2021"
name = "steam-presence"
version = "0.0.0"
authors = ["silvanshade <silvanshade@users.noreply.github.com>"]
license = "Apache-2.0 WITH LLVM-exception"
homepage = "https://github.com/silvanshade/steam-presence"
repository = "https://github.com/silvanshade/steam-presence"
description = """
A system-tray application for reporting Steam status as Discord rich presence
"""
default-run = "steam-presence"

[features]
# by default Tauri runs in production mode
# when `tauri dev` runs it is executed with `cargo run --no-default-features` if `devPath` is an URL
default = ["custom-protocol"]
# this feature is used used for production builds where `devPath` points to the filesystem
# DO NOT remove this
custom-protocol = ["tauri/custom-protocol"]
debug = ["tracing", "tracing-subscriber"]

[build-dependencies]
built = { version = "0.5", features = ["chrono", "git2", "semver"] }
filetime = "0.2"
regex = "1.6"
serde_json = { version = "1.0", default-features = false }
snafu = { version = "0.7", default-features = false, features = ["std"] }
tauri-build = { version = "1.0", features = [] }

[dependencies]
cached = { version = "0.38", default-features = false }
directories = "4.0"
discord-rich-presence = "0.2"
exitcode = "1.1"
serde = { version = "1.0", default-features = false }
serde_json = { version = "1.0", default-features = false }
snafu = { version = "0.7", default-features = false, features = ["std"] }
tauri = { version = "1.0", default-features = false, features = ["devtools", "http-request", "shell-open", "system-tray", "wry"] }
tokio = { version = "1.20", default-features = false }
tracing = { version = "0.1", default-features = false, features = ["attributes"], optional = true }
tracing-subscriber = { version = "0.3", default-features = false, features = ["ansi"], optional = true }

[profile.release]
codegen-units = 1
lto = "fat"
incremental = false
opt-level = "z"
panic = "abort"
strip = true
